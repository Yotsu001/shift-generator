<section class="page-shell page-shell--narrow auth-page">
  <div class="page-header">
    <div>
      <p class="eyebrow">アカウント</p>
      <h1 class="page-title">パスワード再設定</h1>
      <p class="page-subtitle">登録済みメールアドレスへ再設定用の案内を送信します。</p>
    </div>
  </div>

  <section class="card auth-card auth-card--form">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "stacked-form auth-form" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :email, "メールアドレス" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="form-actions">
        <%= f.submit "再設定メールを送る" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </section>
</section>
